
CFLAGS=$(shell pkg-config --libs --cflags cmockery2)

all:hash_test

hash_test:hash.c hash.h  hash_test.c clean_buff.c clean_buff.h  hash_Makefile
	gcc -o  hash_test hash.c hash.h clean_buff.c clean_buff.h hash_test.c  $(CFLAGS)


